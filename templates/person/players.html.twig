{% extends 'base.html.twig' %}

{% block title %}Jugadores de {{ team.name }}{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/cardmain.css') }}">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@latest/dist/tabler-icons.min.css" />
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('javascript/desplegable.js') }}"></script>
{% endblock %}

{% block header %}
    {% include 'navbar.html.twig' %}
{% endblock %}

{% block body %}
    <div class="cabecera-crud">
        <div>
            <h2 class="h2-titulo">
                Equipo {{ team.name }}
            </h2>
        </div>
        <div class="contenedor-derecha">
            <div class="contenedor-jugador-deporte">
                <h2>{{ team.sport.name }}</h2>
            </div>

        </div>
    </div>
    <div id="listar">
        {% for person in people %}
            <div class="contenedor-principal">
                <div class="contenedor-expandir">
                    <div class="expandible">
                        {% if person.isPlayer == 1 and person.isTeacher == 0 %}
                            <span class="titulo"><h6>{{ person.firstName }} {{ person.lastName }} - Jugador </h6></span>
                        {% else %}
                            <span class="titulo"><h6>{{ person.firstName }} {{ person.lastName }} - Profesor</h6></span>
                        {% endif %}
                        <div class="iconos">
                            <span class="desplegar"><i class="ti ti-chevron-down a-botones"></i></span>
                        </div>
                    </div>
                    <div class="contenedor-desplegable">
                        <div class="contenedor-informacion-desplegable">
                            <div class="informacion">
                                <h3>Nombre</h3>
                                <p>{{ person.firstName }}</p>
                            </div>
                            <div class="informacion">
                                <h3>Apellidos</h3>
                                <p>{{ person.lastName }}</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
    <div class="contenedor-footer">
        <div class="volver">
            <a href="{{ path('people') }}" class="boton-volver"><i class="ti ti-arrow-left icono-agregar"></i> Volver</a>
        </div>
    </div>
{% endblock %}