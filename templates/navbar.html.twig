{% set current_path = app.request.get('_route')|split('_') %}
<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" style='font-size: 3rem; font-weight: bold' href="{{path('main_app')}}"> Liga Escolar</a>
        {# <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="#">1</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">2</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">3</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">4</a>
                </li>
            </ul>
        </div> #}
        <div class="col-auto" id="navbarNav">
            {% if app.user %}
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link {{ current_path[0] == 'principal' ? 'active" aria-current="page' }} text-white"
                        ><i class="ti ti-user-filled"></i>Conectado como {{ app.user.userName }}</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('logout') }}" class="btn btn-secondary">Desconectar <i class="ti ti-user-off"></i></a>
                    </li>
                </ul>
            {% endif %}
        </div>
    </div>
</nav>

